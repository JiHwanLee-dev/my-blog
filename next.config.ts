// next.config.ts → Next.js 프레임워크 자체의 동작 방식을 커스터마이징하는 설정 파일입니다.
// 이미지 도메인 허용, 환경변수 설정, 리다이렉트 규칙 등 고급 설정을 여기서 합니다.
// 처음에는 거의 건드릴 일이 없습니다.

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* 필요한 설정 옵션을 여기에 추가합니다 */
};

export default nextConfig;
